#include <cstdio>
#include <cstring>
#include <vector>
using namespace std;

int t, n, m;

int main() {
	scanf("%d", &t);
	while (t--) {
		scanf("%d%d", &n, &m);
		int u, v;
		while (m--) {
			scanf("%d%d", &u, &v);
		}
	}
	return 0;
}
