#include <cstdio>
#include <cstring>

const int N = 3005;
int t, n, a;

int main() {
    scanf("%d", &t);
    while (t--) {
	scanf("%d", &n);
	for (int i = 0; i < n; i++) {
	    scanf("%d", a);
	}
    }
    return 0;
}
